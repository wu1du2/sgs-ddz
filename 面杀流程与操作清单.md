# 面杀流程与操作清单（手动执行版）

> 目的：放弃自动执行复杂卡牌效果，系统仅实现清晰确定的基础逻辑；其余效果由玩家自行决定与操作，尽量贴合真实面杀体验。

## 一局面杀的阶段与玩家操作

### 0. 开局与准备
- 选模式/座次/身份（如斗地主、身份局等）。
- 武将选择与展示（含体力上限、初始体力、技能栏）。
- 构建与洗牌（标准/军争等牌堆）。
- 发初始手牌。

**玩家操作（面杀）**
- 选择座位与确认身份。
- 选择武将（多人候选、地主额外候选等）。
- 确认起始体力与体力上限。
- 执行洗牌与发牌。

---

### 1. 回合流程（标准 CB）
1) **准备阶段**：技能触发、状态检查。
2) **判定阶段**：处理乐不思蜀/兵粮寸断等延时锦囊。
3) **摸牌阶段**：摸 2 张牌（或技能改变摸牌数）。
4) **出牌阶段**：使用牌/发动技能/指定目标。
5) **弃牌阶段**：手牌数 > 当前体力则弃至体力值。
6) **结束阶段**：技能触发与状态清理。

**玩家操作（面杀）**
- 自行宣告阶段开始与结束。
- 自行翻判定牌、执行判定结果。
- 自行摸牌、出牌、弃牌。

---

### 2. 关键操作类型（面杀常见）

#### 发牌/摸牌/弃牌
- 摸牌：从牌堆顶抽牌到手牌。
- 发牌：对其他玩家或公共区域发牌。
- 弃牌：从手牌/装备/判定区移至弃牌堆。

#### 出牌/响应
- 使用牌：杀、闪、桃、酒、锦囊、装备等。
- 响应牌：如闪响应杀、无懈可击响应锦囊。

#### 牌的移动
- 偷牌/顺牌：从他人手牌/装备区获得牌。
- 移动判定区牌（如转移延时锦囊）。
- 控牌堆顶：观看/置顶/置底牌堆顶若干张。
- 控牌堆底：将牌放到底部或从底部取牌。

#### 体力变化
- 扣血：受到伤害（1 点或多点）。
- 回血：回复体力。
- 濒死：体力 ≤ 0 时进入濒死救援。
- 死亡：无法被救援或条件触发阵亡。

#### 状态变化
- 翻面/重置（翻面武将的状态切换）。
- 连环/重置（铁索连环状态）。
- 横置/解除（视规则/房规）。

#### 牌堆管理
- 洗牌：弃牌堆回收成新牌堆。
- 牌堆检查：查看剩余牌数。

---

## 1. 系统需要实现的逻辑（最小且确定）

### A. 牌堆与牌的确定性操作
- 初始化牌堆（标准/军争等预置构成）。
- 洗牌（随机打乱）。
- 摸牌/发牌（从顶抽取）。
- 弃牌入弃牌堆、弃牌堆计数。
- 牌堆耗尽时触发洗牌（弃牌堆回收）。
- 牌堆顶/底操作（明确执行的置顶/置底/查看）。

### B. 手牌与区域管理
- 维护每位玩家手牌数量。
- 维护装备区与判定区的牌列表。
- 明牌展示区（最近打出的牌）。

### C. 体力与状态基础数值
- 体力上限与当前体力。
- 扣血/回血（数值变化）。
- 濒死标记与死亡标记。
- 翻面/连环等状态标记。

### D. 回合与阶段
- 当前玩家指示。
- 阶段切换（准备/判定/摸牌/出牌/弃牌/结束）。
- 回合顺序推进。

---

## 2. 需要向用户暴露的接口（手动驱动）

### A. 牌堆相关接口
- `shuffleDeck()`：洗牌。
- `drawCards(playerId, count)`：给指定玩家摸牌。
- `dealToCenter(count)`：发牌到公共展示区。
- `moveToDiscard(cards[])`：将牌移入弃牌堆。
- `peekTop(count)`：查看牌堆顶若干张。
- `setTop(cards[])`：将若干张牌置顶。
- `setBottom(cards[])`：将若干张牌置底。

### B. 玩家区域接口
- `giveCards(from, to, cards[])`：玩家间移牌。
- `stealCards(from, to, count | cards[])`：偷牌/顺牌。
- `equipCard(playerId, card)`：装备牌。
- `unequipCard(playerId, card)`：卸下装备。
- `moveJudgeCard(from, to, card)`：转移判定区牌。

### C. 体力与状态接口
- `damage(playerId, amount)`：扣血。
- `heal(playerId, amount)`：回血。
- `setDead(playerId, bool)`：死亡标记。
- `setTurnedOver(playerId, bool)`：翻面状态。
- `setChained(playerId, bool)`：连环状态。

### D. 回合与阶段接口
- `setActivePlayer(playerId)`：指定当前行动玩家。
- `nextTurn()`：切换到下一个玩家。
- `setPhase(phase)`：切换阶段。

### E. 明牌与日志
- `revealCard(card)`：公共明牌区展示。
- `logAction(text)`：记录玩家宣告的动作（便于还原面杀过程）。

---

## 4. 拖拽式交互方案（作为默认 UI 方向）

### 4.1 核心原则
- 所有“搬牌”动作通过拖拽完成，尽量减少按钮。
- 系统仅执行确定性的移动与数值更新，不判定复杂规则。
- 他人手牌保持隐私：只能随机抽取，不显示牌面。

### 4.2 可拖拽对象与区域
- 可拖拽对象：
  - 自己手牌（可见牌面，单张/多选拖拽）。
  - 他人手牌占位牌（背面，代表“随机抽取 1 张”）。
  - 牌堆顶占位牌（背面，代表“摸 1 张”）。
  - 弃牌堆顶牌（明牌，点击后可以查看所有卡牌）。
  - 装备区/判定区牌（明牌）。
- 可放置区域：
  - 玩家手牌区、弃牌区、装备区、判定区。
  - 牌堆顶、牌堆底。
  - 明牌区（翻面展示区）。
  - 放回牌堆顶/牌堆底占位（空卡位，用于把任意区域的牌放回顶/底）。

### 4.3 典型拖拽动作映射
- 偷牌：拖拽“他人手牌占位牌” → 放到自己手牌区。
- 弃牌：拖拽“自己手牌” → 放到弃牌区。
- 摸牌：拖拽“牌堆顶占位牌” → 放到自己手牌区。
- 移动装备/判定：拖拽“装备/判定区牌” → 放到目标区域。
- 控牌堆顶/底：拖拽“明牌/手牌” → 放到牌堆顶/底。
- 翻牌公开：拖拽“任意区域背面牌” → 放到明牌区，自动翻为正面。
- 明牌再流转：从明牌区拖拽到任意区域。

### 4.4 隐私与随机规则
- 他人手牌仅以背面牌堆呈现。
- 拖拽他人手牌占位牌 = 系统随机抽取 1 张。
- 需要随机多张时：拖拽后弹出数量选择（如 1/2/3）。
- 指定牌仅允许从“明牌/装备/判定区”选择。
- 背面牌进入明牌区即翻为正面，后续可被指定与再次移动。

### 4.5 拖拽反馈与日志
- 拖拽时显示操作提示浮层：来源 → 目标 → 数量。
- 可放置区域高亮，非法区域灰置。
- 操作落地后写入日志与 Toast 提示。

### 4.6 撤销与冲突
- 单操作原子化：同一时刻只允许一个拖拽操作生效。
- 允许撤销“最近一次操作”（限时/限一次）。

### 4.7 仍需保留的少量按钮
- 新开局、下一回合、阶段切换、撤销/取消。
- 体力变化与状态切换（翻面、连环、死亡）仍使用按钮。

### 4.8 UI 表现（卡牌外观与占位）
- 卡牌外观：使用扑克牌形状的卡片占位（圆角矩形），文本显示“花色 + 点数 + 卡牌名”。
- 背面牌：统一背面样式，不显示花色点数与名称。
- 明牌区：展示正面牌，支持再次拖拽。
- 牌堆顶/底占位：显示为空卡位，用作拖拽落点。


## 3. 面杀一致性的设计原则
- 系统只做“确定性动作”，不做复杂效果判断。
- 所有效果由玩家宣告，系统仅协助移动牌/修改数值。
- 明确提示“谁做了什么”，但不验证“是否符合规则”。
- 允许房规：例如允许手动调血、手动判定、手动强制死亡等。