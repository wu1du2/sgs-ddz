# 3 人好友联机卡牌游戏需求与详细设计

## 目标与范围
- 目标：实现一个 3 人好友联机卡牌游戏，玩法完全照抄《三国杀移动版》的“斗地主模式”。
- 范围：仅用于熟人游玩，不需要复杂的安全校验、注册登录逻辑。
- 技术栈：`Vite + Vue + TypeScript + Node.js`。
- 部署：可在 Render 在线部署（前端静态资源 + 后端服务）。
- 约束：服务端内存存储，不使用数据库和磁盘存储。

## 基本规则与完整流程（斗地主模式）
> 以下规则以移动版三国杀斗地主为基准进行整理，便于后续实现。

### 角色与胜利条件
- 3 名玩家：1 名地主、2 名农民。
- 胜利条件：
  - 地主胜利：两名农民全部阵亡。
  - 农民胜利：地主阵亡。

### 武将牌与势力
- 每位玩家选择 1 名武将（简化实现可固定小型武将池）。
- 武将属性：
  - 体力上限（HP Max）
  - 初始体力（通常等于体力上限）
  - 技能（斗地主中常见技能，可先实现基础/通用技能）

### 卡牌类型与基础效果
- 手牌：每位玩家拥有手牌区。
- 装备牌：武器、防具、坐骑、宝物（可按移动版斗地主的简化配置）。
- 锦囊牌：
  - 延时锦囊：判定后生效。
  - 非延时锦囊：使用后立即结算。
- 基础牌：如“杀”“闪”“桃”等。

#### 基础牌示例（最小实现建议）
- **杀**：对一名角色造成 1 点伤害，目标可出“闪”抵消。
- **闪**：响应“杀”，抵消伤害。
- **桃**：回复 1 点体力（濒死时也可使用）。

#### 伤害与濒死
- 体力降至 0 或以下进入濒死状态。
- 濒死：轮流询问所有玩家是否使用“桃”救援。
- 无人救援则角色阵亡。

### 回合流程（标准回合阶段）
1. **准备阶段**：触发准备类技能或效果。
2. **判定阶段**：处理延时锦囊的判定与生效。
3. **摸牌阶段**：默认摸 2 张牌。
4. **出牌阶段**：玩家可以打出牌、使用技能。
5. **弃牌阶段**：手牌数 > 当前体力，弃至体力值。
6. **结束阶段**：触发结束类技能或效果。

### 斗地主流程
1. **房间创建与加入**：3 人进入同一房间。
2. **选将与身份分配**：
   - 随机身份：1 地主、2 农民。
   - 简化：身份全明视。一开始可以强制玩家轮流当地主，后续可以通过叫分来选择地主。
3. **初始手牌发放**：按规则给地主与农民发不同数量的初始牌（可配置）。
4. **游戏进行**：按座次依次回合。
5. **结束判定**：地主或农民一方满足胜利条件，游戏结束。

## 系统设计（极简内存版）

### 架构概览
- 前端：`Vue + TypeScript` 负责 UI 和用户交互。
- 后端：`Node.js` 作为游戏房间与状态同步服务。
- 通信：`WebSocket` 实时同步房间与对局状态。
- 部署：
  - 前端：构建后静态资源。
  - 后端：提供 WebSocket 服务与少量 HTTP 接口。

### 数据存储策略
- 全部数据存放在服务端内存：
  - 房间状态
  - 玩家状态
  - 牌堆与弃牌堆
  - 回合与阶段信息
- 断线后可尝试重连恢复（短时间内保留房间内存数据）。
- 不使用数据库或磁盘持久化。

## 关键数据结构（概念级）

### 房间状态
- 房间 ID
- 玩家列表（3 人）
- 当前回合玩家索引
- 游戏阶段（准备、判定、摸牌、出牌、弃牌、结束）
- 牌堆、弃牌堆、判定区

### 玩家状态
- 玩家 ID、昵称
- 身份（地主/农民）
- 武将信息（体力上限、当前体力、技能）
- 手牌、装备区、判定区

### 卡牌模型
- 卡牌 ID
- 类型（基础/装备/锦囊）
- 名称（杀/闪/桃等）
- 目标规则
- 效果结算描述

## 交互与 UI 设计（移动端优先）

### UI 目标
- 适配主流手机分辨率（375×667、390×844、412×915 等）。
- 单手操作友好：关键按钮靠近屏幕底部。
- 角色状态清晰：体力、手牌数、装备区一目了然。

### 界面布局建议
- 顶部：房间信息、阶段提示。
- 中部：对手区域 + 自己区域。
- 底部：手牌区域（横向滑动）。
- 右侧/底部：出牌、结束回合、取消等操作按钮。

### 交互方式
- 点击手牌高亮，二次点击确认使用。
- 目标选择：点击目标玩家头像。
- 结算动画与提示：使用轻量浮层提示（如“出杀”“濒死”“救援成功”）。

## 服务端逻辑（概念流程）

### 房间与对局生命周期
1. 创建房间 -> 生成房间 ID。
2. 3 人加入 -> 自动开始选将与身份分配。
3. 初始化牌堆、发牌 -> 进入第一个回合。
4. 游戏过程中通过 WebSocket 广播状态变化。
5. 结束后房间销毁或进入结算页面。

### WebSocket 消息类型（概念级）
- `room:create` / `room:join`
- `game:start` / `game:state`
- `player:action`（出牌、发动技能、响应）
- `game:phase`（阶段切换）
- `game:end`

## 可配置项（后续实现时可调整）
- 初始手牌数（地主与农民不同）
- 武将池范围（简化版/完整版）
- 牌堆构成

## 交付物说明
- 本文档用于需求确认与设计评审。
- 确认后再进入实际编码阶段。

## 第一期实现策略：人工操作驱动（不自动化技能与结算）

### 设计原则
- 不做自动化规则与技能结算，由玩家主动发起操作。
- 服务端仅做轻量校验与状态维护，保证房间一致性与数据安全边界。
- 操作均通过前端界面触发，服务端串行处理并广播最新状态。

### 至少需要实现的功能清单

#### 房间与会话
- 创建/加入/离开房间（3 人上限）。
- 房主指定或随机分配身份（地主/农民）。
- 选将与确认（支持房主强制指定，以避免纠纷）。
- 开始/结束对局（支持中途终止、重新开始）。

#### 回合与阶段（完全手动）
- 设置座次与当前行动玩家。
- 手动切换阶段：`准备 → 判定 → 摸牌 → 出牌 → 弃牌 → 结束`。
- 手动结束回合并切换至下一玩家。

#### 牌堆与卡牌管理（手动运维）
- 初始化/重置牌堆（洗牌、构建基本牌、锦囊、装备）。
- 从牌堆顶摸 N 张到指定玩家手牌。
- 将指定手牌或装备移至弃牌堆。
- 在玩家之间移动卡牌：
  - 从 A 的手牌随机拿走 1 张到 B 的手牌。
  - 将装备/判定区卡牌在玩家之间转移（有效性由玩家自行约定）。

#### 玩家状态操作接口
- 扣血：对指定玩家减少体力（支持指定数值）。
- 回血：对指定玩家增加体力，最多不超过体力上限。
- 濒死与救援流由玩家手动操作（如在濒死时多人依次选择是否使用`桃`）。
- 标记死亡：设置玩家为阵亡状态（自动清理可选：将其手牌与装备移入弃牌堆）。
- 标记胜利：房主或一致同意后，手动判定地主或农民一方胜利并结束对局。

#### 身份与武将管理
- 设置/修改玩家身份（地主/农民）。
- 选择/变更武将与体力上限、初始体力调整（用于失误修正）。

#### 轻量校验与保护
- 手牌移动合法性：不能从不存在的卡池拿取，也不能丢弃未拥有的卡。
- 体力合法性：回到不超过上限，扣至低于 0 即提示濒死（不强制自动救援）。
- 死亡玩家默认不可再出牌或执行操作（除非被手动复活）。
- 随机拿牌时基于当前手牌数量抽取，不暴露具体手牌信息。
- 串行处理同房间操作以避免并发冲突（操作队列）。

#### 广播与记录
- 广播最新房间与对局状态：玩家列表、身份、阶段、牌堆/弃牌堆规模、各玩家区域。
- 操作日志：记录每一步操作的发起者、目标、参数、结果（便于回看与纠错）。
- 轻量撤销：仅允许房主在短时间窗口撤销最近一次操作（可选）。

### 前端交互与 UI（移动端友好）
- 动作面板：
  - 玩家操作：`摸牌`、`弃牌`、`拿牌（从他人）`、`扣血`、`回满/回 X`、`标记死亡`、`标记胜利`。
  - 局面操作：`切阶段`、`结束回合`、`设置当前行动者`、`重置牌堆`。
- 目标选择：点击头像选择目标；多选支持（例如从多人处分别拿牌）。
- 卡牌选择：手牌区支持多选与随机选择（随机由服务端执行）。
- 安全确认：对关键操作弹确认（扣血、死亡、胜利判定、跨玩家拿牌）。
- 适配：底部主操作区、大按钮；状态信息浓缩显示；支持 375–430 宽度断点。
- 反馈：轻量提示（Toast）与动画（例如牌移动轨迹）。

### 事件/接口草案（不编码，仅命名与参数示意）
- 房间与基础：
  - `room:create { roomId? }`
  - `room:join { roomId, playerName }`
  - `room:leave { roomId }`
  - `game:start { roomId }`
  - `game:end { roomId, winner: 'lord' | 'farmers' }`
- 身份与武将：
  - `player:setRole { playerId, role }`
  - `player:setGeneral { playerId, generalId, hpMax, hp }`
- 回合与阶段：
  - `turn:setActive { playerId }`
  - `phase:set { phase }`
  - `turn:end {}`
- 牌堆与卡牌：
  - `deck:reset { configId? }`
  - `card:draw { playerId, count }`
  - `card:discard { playerId, cardIds[] }`
  - `card:moveBetweenPlayers { fromId, toId, count | cardIds[] , zone: 'hand'|'equip'|'judge' }`
  - `card:moveToDiscard { playerId, cardIds[] | fromZone }`
  - `card:recoverFromDiscard { toPlayerId, count | cardIds[] }`
- 生命与状态：
  - `hp:damage { playerId, amount }`
  - `hp:heal { playerId, amount }`
  - `player:setDead { playerId, dead: true }`
  - `game:setWinner { side: 'lord' | 'farmers' }`
- 广播：
  - `game:state { ...完整房间与对局状态 }`
  - `log:append { action, by, payload, timestamp }`

### 并发与一致性
- 服务端为房间维护单线程操作队列，依序处理事件，保证状态一致。
- 冲突策略：对同一时刻的互斥操作（例如两人同时对同一手牌进行移动）由队列顺序决定结果，后者收到失败或重试提示。

### 最小测试清单（上线前自验）
- 三人建房、进房、选将、分配身份、开始对局。
- 手牌摸/弃/跨人拿、弃牌堆进出、装备区移动。
- 扣血、回血、濒死提示、死亡标记与胜利标记。
- 回合与阶段手动切换、状态广播一致。
- 移动端交互流畅（≤150ms 交互反馈），主要机型分辨率显示正确。